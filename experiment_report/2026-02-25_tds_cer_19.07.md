# Experiment report: Current setup — TDS baseline, CER 19.07

## Summary

| Field | Value |
|-------|--------|
| **Date** | 2026-02-25 |
| **Model** | TDSConvCTCModule (emg2qwerty baseline) |
| **Best val CER** | **19.07399%** |
| **Config** | Default: model=tds_conv_ctc, user=single_user, optimizer=adam |

---

## Model and code

- **Architecture:** SpectrogramNorm → MultiBandRotationInvariantMLP → TDSConvEncoder → Linear → LogSoftmax (CTC).
- **Config:** `model=tds_conv_ctc`, `user=single_user`, `decoder=ctc_greedy`, `lr_scheduler=linear_warmup_cosine_annealing`, `optimizer=adam`.

---

## Training setup (current config)

| Setting | Value |
|---------|--------|
| max_epochs | 150 |
| batch_size | 32 |
| optimizer | Adam (config/optimizer/adam.yaml, lr=1e-3) |
| lr_scheduler | LinearWarmupCosineAnnealingLR (pl_bolts) |
| monitor_metric | val/CER (min) |
| seed | 1501 |
| accelerator | GPU |

**TDS (config/model/tds_conv_ctc.yaml):** in_features=528, mlp_features=[384], block_channels=[24,24,24,24], kernel_width=32; window_length=8000, padding=[1800,200].

---

## Metrics

- **Best validation CER:** 19.07399

---

## Notes

- Recorded as current baseline for comparison (e.g. vs AdamW or CNN+Transformer).
